export enum Sys_Role {
  user = 'user',
  admin = 'admin',
  company_admin = 'company_admin',
}

export enum Gender {
  male = 'male',
  female = 'female',
}
export enum maritalStatus {
  single = 'single',
  married = 'married',
  divorced = 'divorced',
  widower = 'widower',
}
export enum MilitarySituation {
  exempt = 'exempt',
  completed = 'completed',
  postponed = 'postponed',
}
export enum skillsLevel {
  beginner = 'beginner',
  intermediate = 'intermediate',
  advanced = 'advanced',
}
export enum File {
  CV = 'CV',
  image = 'image',
}
