export enum Location {
  remote = 'remote',
  onsite = 'onsite',
  hybrid = 'hybrid',
}
export enum jobType {
  part_time = 'part_time',
  full_time = 'full_time',
}

export enum JobStatus { 
  open = "open",
  closed = "closed",
}